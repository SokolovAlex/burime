import styled from 'styled-components';

import { Readme } from '@styled-icons/fa-brands/Readme';
import { colors } from '../../../constants';

export const ReadmeStyled = styled(Readme)`
    cursor: pointer;
    color: ${colors.primary};
`;